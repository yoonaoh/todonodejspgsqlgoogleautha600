name: Trigger auto deployment for container-app-fewrnpq5h3hba

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-fewrnpq5h3hba-AutoDeployTrigger-f49daca4-3304-43e4-90ed-ad1e2de24948.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPFEWRNPQ5H3HBA_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPFEWRNPQ5H3HBA_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPFEWRNPQ5H3HBA_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPFEWRNPQ5H3HBA_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPFEWRNPQ5H3HBA_REGISTRY_PASSWORD }}
          containerAppName: container-app-fewrnpq5h3hba
          resourceGroup: todonodejspgsqlgoogleautha6008334
          imageToBuild: default/container-app-fewrnpq5h3hba:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


